apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: gwc
    type: data
  name: geowebcache-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: gp3  # Cambiar a una clase de almacenamiento de EKS
